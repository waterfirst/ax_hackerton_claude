# DIARY 2026-07-16

## 오늘 한 일
- 시가 엔진에 재귀적 자기수정 레이어를 추가했다.
- `adaptive.py`를 신규 생성해 `K_EWY`, `CRISIS_K`, `BLOWOFF_COEF`를 채점오차 기반으로 미세조정하도록 만들었다.
- `core.py`에 VKOSPI 위기레짐과 적응계수 배선을 반영했다.
- `kospi_battle.py`에서 시가 채점 후 `adaptive.learn()`이 자동 호출되도록 연결했다.
- `tests/test_adaptive.py`를 추가하고, `tests/test_core.py`에 적응형 상태파일 무시 안전장치를 넣었다.

## 검증
- `python tests/test_core.py` 통과
- `python tests/test_adaptive.py` 통과
- 하위호환성 유지: 상태파일이 없으면 기존 v8과 동일 동작
- 위기레짐에서 `CRISIS_K`가 오차 축소 방향으로 이동함을 확인

## 배운 것
- 적응형 레이어는 강하지만, 종결조건과 클램프가 먼저 없으면 쉽게 발산한다.
- 테스트는 `pytest`가 없어도 직접 실행형으로 유지하면 환경 의존성을 줄일 수 있다.
- 실제 자동학습은 느리게 수렴해야 안전하다. 빠른 적응보다 발산 방지가 우선이다.

## 다음 할 일
- 2026-07-17 07:35 라이브는 순수 v8 상태로 시작하는지 확인
- 16:35 채점 후 `records/adaptive_log.jsonl` 생성 여부 확인
- 2주·1개월 리플레이로 적응 속도와 안정성 추가 검증
